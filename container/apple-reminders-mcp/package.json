{
  "name": "apple-reminders-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "Apple Reminders MCP server (IPC bridge for NanoClaw containers)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
